name: Daily News Bot

on:
  schedule:
    # UTC 00:00 = 台灣時間 08:00
    - cron: "0 0 * * *"
  workflow_dispatch: # 允許手動觸發，方便測試

jobs:
  run-news-bot:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Cache pip dependencies
        uses: actions/cache@v4
        with:
          path: ~/.cache/pip
          key: ${{ runner.os }}-pip-${{ hashFiles('requirements.txt') }}
          restore-keys: |
            ${{ runner.os }}-pip-

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Run news bot
        env:
          GROQ_API_KEY: ${{ secrets.GROQ_API_KEY }}
          NEWS_API_KEY: ${{ secrets.NEWS_API_KEY }}
          WEBHOOK_TECH_HSUAN: ${{ secrets.WEBHOOK_TECH_HSUAN }}
          WEBHOOK_TECH_BLACK: ${{ secrets.WEBHOOK_TECH_BLACK }}
          WEBHOOK_FINANCE_HSUAN: ${{ secrets.WEBHOOK_FINANCE_HSUAN }}
          WEBHOOK_FINANCE_BLACK: ${{ secrets.WEBHOOK_FINANCE_BLACK }}
          WEBHOOK_POLITICS_HSUAN: ${{ secrets.WEBHOOK_POLITICS_HSUAN }}
          WEBHOOK_POLITICS_BLACK: ${{ secrets.WEBHOOK_POLITICS_BLACK }}
          WEBHOOK_TAIWAN_HSUAN: ${{ secrets.WEBHOOK_TAIWAN_HSUAN }}
          WEBHOOK_TAIWAN_BLACK: ${{ secrets.WEBHOOK_TAIWAN_BLACK }}
        run: python main.py
